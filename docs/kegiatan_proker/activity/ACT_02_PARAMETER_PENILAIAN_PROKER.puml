@startuml ACT_02_PARAMETER_PENILAIAN_PROKER
!theme plain
!pragma useVerticalIf on
scale max 1600 width
skinparam wrapWidth 200

skinparam defaultFontSize 12
skinparam swimlaneWidth 220
skinparam ActivityStartColor black
skinparam ActivityEndColor black
title ACT-02 · Mengelola Parameter Penilaian Proker\nAktor: Admin

|Admin|
|System|

|Admin|
start
:Buka menu Parameter Penilaian Proker;
:Pilih aksi;

if (Aksi?) then (Lihat Daftar Parameter)
  |System|
  :Ambil data parameter penilaian proker;
  :Tampilkan daftar parameter\n(nama, bobot, deskripsi);
  |Admin|
  :Lihat daftar parameter;

elseif (Aksi?) then (Tambah Parameter)
  |Admin|
  :Isi form\n(nama parameter, bobot, deskripsi?);
  |System|
  :Periksa input\n(total bobot = 100);
  if (Form valid?) then (tidak)
    :Tampilkan pesan kesalahan;
    |Admin|
    :Perbaiki form;
  else (ya)
    :Simpan data baru;
    :Tampilkan notifikasi sukses;
    |Admin|
    :Parameter baru tersimpan;
  endif

elseif (Aksi?) then (Edit Parameter)
  |Admin|
  :Klik Edit ? ubah nama/bobot;
  |System|
  :Periksa kelengkapan form;
  if (Form valid?) then (tidak)
    :Tampilkan pesan kesalahan;
  else (ya)
    :Perbarui data;
    :Tampilkan notifikasi sukses;
    |Admin|
    :Parameter diperbarui;
  endif

elseif (Aksi?) then (Hapus Parameter)
  |Admin|
  :Klik Hapus ? konfirmasi;
  |System|
  :Hapus data;
  :Tampilkan notifikasi sukses;
  |Admin|
  :Parameter terhapus;

endif

stop
@enduml
